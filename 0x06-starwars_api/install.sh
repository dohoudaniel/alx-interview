#!/usr/bin/env bash
# This script installs all the required modules to work with this project
sudo npm install request --global
export NODE_PATH=/usr/lib/node_modules
sudo npm install semistandard --global
